version: '3.8'

services:
  backend:
    build:
      context: ./backend
    user: "root"
    expose:
      - "3001"
    environment:
      - NODE_ENV=production
      - ALLOWED_ORIGIN=http://localhost
    volumes:
      - typing_data:/app/data
    restart: always

  frontend:
    build:
      context: ./frontend
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - backend
    volumes:
      - /etc/letsencrypt:/etc/letsencrypt:ro
    restart: always

volumes:
  typing_data:
